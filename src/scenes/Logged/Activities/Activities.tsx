import { Background } from "src/components";

export default function Activities(){
    return (
        <Background>
            <p style={{color: "black"}}>
                This is the activities content;
            </p>
        </Background>
    )
}