import { itemType } from "src/types";
import { colors } from "src/colors";
import { X } from "react-feather";
import { Content, Items, Line} from "./AddList.style";

interface props {}

export default function AddList({}: props){

    return (
        <Items>
        </Items>
    )
}