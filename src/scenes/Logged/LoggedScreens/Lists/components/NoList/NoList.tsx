import { itemType } from "src/types";
import { colors } from "src/colors";
import { X } from "react-feather";
import { Content, Items, Line} from "./NoList.style";

interface props {}

export default function NoList({}: props){

    return (
        <Items>
        </Items>
    )
}